<Window x:Class="PDFSculpt.App.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:PDFSculpt.App.Services"
        xmlns:local="clr-namespace:PDFSculpt.App"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">

    <Window.Resources>
        <conv:ByteArrayToImageConverter x:Key="ByteArrayToImageConverter"/>
    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Orientation="Horizontal" Margin="10">
            <Button Content="Open PDF"
                Command="{Binding OpenPdfCommand}"
                Width="120"
                Margin="0,0,10,0"/>

            <TextBlock Text="{Binding Title}"
                   VerticalAlignment="Center"
                   FontSize="16"/>
        </StackPanel>

        <!-- File Path -->
        <TextBlock Grid.Row="1"
               Text="{Binding SelectedFile}"
               Margin="10,0,10,10"
               FontSize="12"
               TextWrapping="Wrap"/>

        <!-- PDF Viewer -->
        <ScrollViewer Grid.Row="2"
                  VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">

            <ItemsControl ItemsSource="{Binding Pages}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border BorderBrush="#DDD"
                            BorderThickness="1"
                            Margin="10"
                            Padding="10"
                            HorizontalAlignment="Center">

                            <StackPanel>
                                <Image Source="{Binding ImageData, Converter={StaticResource ByteArrayToImageConverter}}"
                                   Width="500"
                                   Stretch="Uniform"/>

                                <TextBlock Text="{Binding DisplayPageNumber}"
                                       HorizontalAlignment="Center"
                                       Margin="0,5,0,0"
                                       FontSize="10"/>
                            </StackPanel>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

        </ScrollViewer>
    </Grid>
</Window>
